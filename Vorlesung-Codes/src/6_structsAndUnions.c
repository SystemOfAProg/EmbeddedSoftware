#include "stdio.h"
#include "string.h"

// typedef {

// } MemoryDevice;

int main(int argc, char* argv[]) {
//     MemoryDevice data;
//     memset(&data, 42, sizeof(data));
//     data.track[2][0] = 43;    // Writes to first byte of track 2
//     data.track[3][7927] = 44; // Writes to last byte of track 3
//     printf("%d\n", data.halftrack[3][0]);    // Outputs 43
//     printf("%d\n", data.halftrack[5][7927]); // Outputs 44
     return 0;
}